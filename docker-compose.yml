version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: ai_goal_journal_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_goal_journal
    ports:
      - "5433:5432"
    volumes:
      - ai_goal_journal_pgdata:/var/lib/postgresql/data

volumes:
  ai_goal_journal_pgdata:
